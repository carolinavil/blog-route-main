export default function PostTitle({ children }) {
  return (
    <h1 className="mb-4 text-left mt-28 text-3xl font-bold leading-tight tracking-tighter md:text-left md:text-4xl md:leading-none lg:text-6xl lg:mt-28 text-balance">
      {children}
    </h1>
  )
}
